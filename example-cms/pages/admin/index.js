import { useEffect } from "react";
import { useSelector } from "react-redux";

const AdminHome = () => {
  // const isAuth = useSelector((state) => state.auth.isAuth);

  // useEffect(() => {
  //   if (!isAuth) {
  //     return window.location.href = "/";
  //   }
  // }, [isAuth]);

  // if (isAuth) {
    
  // }
  return <h1>Admin Anasayfası</h1>;
};

export default AdminHome;
